# הקמת Firebase — שלב אחר שלב

מה שאתה עושה: חמש פעולות בקונסולה, ומעתיק לי בלוק אחד של טקסט.
מה שאני עושה: כל השאר — קבצים, כללי אבטחה, פריסה.

**לא נדרש כרטיס אשראי.** תוכנית Spark היא ברירת המחדל ולא מבקשת אמצעי תשלום.
Firebase לא יעבור לחיוב מעצמו; מעבר ל-Blaze דורש פעולה יזומה שלך.

---

## שלב 1 — יצירת הפרויקט

1. היכנס ל-https://console.firebase.google.com והתחבר עם חשבון Google
2. לחץ **Create a project** (או **Add project** אם כבר יש לך פרויקטים)
3. **שם הפרויקט:** `tripai`

   מתחת לשדה יופיע ה-**Project ID** האמיתי, למשל `tripai-4f2a1`. Firebase מוסיף
   סיומת אקראית כי השם חייב להיות ייחודי בעולם. **רשום אותו לעצמך** — הוא יופיע
   בכתובת האתר ובכל פקודה מכאן והלאה.

4. **Google Analytics: כבה** את המתג

   לא נדרש לשום דבר אצלנו, ומוסיף שלב של יצירת חשבון Analytics שרק מאריך את
   התהליך.

5. **Create project** → המתן כדקה → **Continue**

---

## שלב 2 — Firestore (מסד הנתונים)

1. בתפריט הצד: **Build → Firestore Database**
2. **Create database**

### איזה סוג מסד נתונים

לפי גרסת הקונסולה ייתכן שתישאל אחת מהשאלות הבאות, או שתיהן:

**Cloud Firestore או Realtime Database?** → **Cloud Firestore**

`db.js` כתוב מול Firestore SDK. Realtime Database הוא עץ JSON יחיד עם API שונה
לחלוטין — שום דבר לא יעבוד מולו.

**Native mode או Datastore mode?** → **Native mode**

זו לא העדפה אלא דרישה:

| | Native mode | Datastore mode |
|---|---|---|
| SDK לדפדפן | יש | **אין** |
| מאזינים בזמן אמת (`onSnapshot`) | יש | **אין** |
| מיועד ל- | web ומובייל | שרתים, App Engine |

בלי Native mode אין `watchRoutes()` — כלומר אין סנכרון בזמן אמת בין מי שהצטרף
דרך וואטסאפ, ובכלל אי אפשר לפנות למסד מהדפדפן.
**לא ניתן להמיר מסד נתונים בין המצבים אחרי היצירה.**

**Database ID?** → השאר `(default)`. הקוד קורא ל-`getFirestore(app)` בלי שם.

### בחירת מצב אבטחה — כאן טועים

יוצגו שתי אפשרויות:

| אפשרות | מה זה עושה |
|---|---|
| **Test mode** | פותח קריאה וכתיבה **לכל אחד באינטרנט** למשך 30 יום |
| **Production mode** ✅ | חוסם הכל כברירת מחדל |

**בחר Production mode.** הכללים שלנו ב-`firebase.rules` יחליפו את ברירת המחדל
ויפתחו בדיוק את מה שצריך. Test mode היה משאיר את הנתונים שלך חשופים לגמרי.

אל תדאג שזה "חוסם הכל" — עד שאפרוס את הכללים, הקוד פשוט ימשיך לעבוד מול
localStorage.

### בחירת אזור — זו החלטה קבועה

**האזור לא ניתן לשינוי אחרי היצירה.** כדי להחליף צריך למחוק את מסד הנתונים
וליצור אותו מחדש.

בחר לפי הסדר הזה:

1. `me-west1` (תל אביב) — אם מופיע ברשימה, זה הקרוב ביותר והמהיר ביותר
2. `europe-west1` (בלגיה) — חלופה טובה, אזור יחיד
3. `eur3` (אירופה, רב-אזורי) — עמיד יותר לתקלות, יקר יותר מעל המכסה החינמית

לשימוש שלנו כל השלושה בתוך המכסה החינמית. אם `me-west1` שם — קח אותו.

3. **Create** → המתן להקמה

---

## שלב 3 — Anonymous Authentication

זה מה שנותן לכל מכשיר מזהה קבוע בלי לבקש מאף אחד להירשם, וזה מה שמאפשר לכללי
האבטחה להצמיד נתונים למשתמש.

1. בתפריט הצד: **Build → Authentication**
2. **Get started**
3. לשונית **Sign-in method**
4. ברשימת הספקים גלול למטה אל **Anonymous** (תחת *Native providers*)
5. לחץ עליו → הפעל את המתג **Enable** → **Save**

אמור להופיע **Enabled** בירוק לצד Anonymous.

> אל תפעיל שום ספק אחר. Google, Email, Phone — כולם מיותרים כאן ורק מוסיפים
> משטח תקיפה.

---

## שלב 4 — רישום אפליקציית Web

1. לחץ על ⚙️ (גלגל השיניים) ליד **Project Overview** → **Project settings**
2. גלול למטה אל **Your apps**
3. לחץ על אייקון **`</>`** (Web)
4. **App nickname:** `TripAI Web`
5. **אל תסמן** את "Also set up Firebase Hosting for this app"

   נפרוס דרך ה-CLI, ו-`firebase.json` כבר מוכן בריפו. הסימון כאן רק יוסיף שלב
   שנצטרך לבטל.

6. **Register app**

---

## שלב 5 — העתקת ה-config

יופיע מסך עם קטע קוד. מה שצריך הוא הבלוק הזה:

```js
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "tripai-xxxxx.firebaseapp.com",
  projectId: "tripai-xxxxx",
  storageBucket: "tripai-xxxxx.firebasestorage.app",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abc123def456"
};
```

**העתק אותו כמו שהוא ותדביק לי בצ'אט.** אני אכניס את הערכים לקבצי הסביבה.

אם סגרת את המסך: **Project settings → Your apps → SDK setup and configuration →
Config**.

### למה מותר להדביק את המפתח הזה

בניגוד למפתח Gemini, ה-`apiKey` של Firebase **מיועד להיות ציבורי**. הוא רק מזהה
לאיזה פרויקט לפנות, ולא מעניק שום הרשאה בפני עצמו. מה שמגן על הנתונים הם כללי
האבטחה ב-`firebase.rules`, שנאכפים בשרת של Google.

זו הסיבה שהוא מופיע בגלוי בקוד של כל אתר שמשתמש ב-Firebase.

---

## מה קורה אחרי שתשלח לי את ה-config

1. אכניס את הערכים ל-`.env.local`
2. אפרוס את `firebase.rules` — מרגע זה הנתונים מוגנים לפי uid
3. נריץ מקומית ונוודא שהפרופיל נשמר בענן
4. `npm run build` ו-`firebase deploy` — האתר עולה לאוויר
5. תקבל כתובת `https://<project-id>.web.app`

הצ'אט יהיה במצב הדגמה עד שנקים את ה-Worker. זו החלטה מכוונת: כך מפתח ה-AI לא
נוגע באף פריסה ציבורית.

---

## מכסות התוכנית החינמית

| משאב | מכסה |
|---|---|
| Firestore — אחסון | 1 GB |
| Firestore — קריאות | 50,000 ליום |
| Firestore — כתיבות | 20,000 ליום |
| Hosting — אחסון | 10 GB |
| Hosting — תעבורה | 360 MB ליום |
| Authentication | ללא הגבלה |

לקבוצת טיול זה הרבה מעבר למספיק. אם בכל זאת תחרוג — ב-Spark השירות נעצר עד
תחילת החודש הבא. **הוא לא גולש לחיוב.**

---

## תקלות אפשריות

| מה קרה | מה לעשות |
|---|---|
| "Project ID already exists" | Firebase יציע סיומת אחרת — קבל אותה |
| לא מוצא **Anonymous** ברשימה | גלול; הוא תחת *Native providers*, מתחת ל-Email ו-Phone |
| נבחר אזור שגוי ל-Firestore | לפני שיש נתונים — מחק את מסד הנתונים ב-Project settings וצור מחדש |
| נבחר בטעות **Test mode** | לא נורא. פריסת הכללים שלי תדרוס את זה מיד |
| הקונסולה מבקשת אמצעי תשלום | אתה לא ב-Spark. ודא שלא לחצת **Upgrade** בשום שלב |
