# TripAI בחינם — מה עובד, מה מדומה, ומה יעלה כסף

מסמך שמרכז את כל השאלות של "בלי לשלם".

---

## מצב נוכחי: 0 ₪, בלי אף מפתח

| רכיב | מצב | עלות |
|---|---|---|
| אירוח | Firebase Hosting, תוכנית Spark | חינם, בלי כרטיס אשראי |
| מפות | אריחי CARTO Dark (נתוני OpenStreetMap) | חינם, בלי מפתח, בלי הרשמה |
| ניווט | קישור יוצא ל-Google Maps | חינם |
| שיתוף בוואטסאפ | קישור `wa.me` | חינם |
| סוכן AI | **מדומה** — תשובות קבועות | חינם |
| סנכרון בין משתמשים | **מדומה** — state מקומי | חינם |

שתי השורות המסומנות הן היחידות שעדיין לא אמיתיות. הפירוט למטה.

---

## 1. מפות — למה לא Google

Google Maps Static API דורש חשבון חיוב עם כרטיס אשראי **גם בתוך המכסה החינמית**.
בלי billing מקושר כל תמונה חוזרת עם סימן מים אפור "For development purposes only".

לכן האפליקציה משתמשת ב-**CARTO Dark Matter** — אריחי raster שנבנים מנתוני
OpenStreetMap. בלי מפתח, בלי הרשמה, בלי כרטיס. המתמטיקה של Web Mercator נמצאת
ב-[src/lib/tiles.js](src/lib/tiles.js), וההרכבה ב-[MapCanvas.jsx](src/components/MapCanvas.jsx).

כפתור השכבות במפה מחליף בין שלושה ספקים חינמיים: CARTO כהה, CARTO כהה ללא
תוויות, ו-OpenStreetMap בהיר.

**חובה משפטית:** קרדיט לספק. הוא מוצג בפינת המפה — אל תסיר אותו.

**מגבלת שימוש הוגן:** אלה endpoints ציבוריים חינמיים. מצוין לאב-טיפוס
ולאפליקציה בהיקף סביר. לתנועה גבוהה מצפים שתירשם (ל-CARTO, MapTiler או Stadia
יש כולם free tier) או תארח אריחים בעצמך.

### ומה לגבי להוריד מפות מ-Google Maps?

**לא. זו הפרה של תנאי השימוש של Google.** סעיף "No Scraping" ו-"No caching or
storage" בתנאי Google Maps Platform אוסרים במפורש להוריד אריחים, לשמור צילומי
מסך של המפה, או להשתמש בתוכן המפה מחוץ לשירותי Google. זה נכון גם לצילום מסך
ידני וגם לסקריפט שמוריד אריחים. מעבר לסיכון המשפטי, זה גם לא יעבוד טוב —
מקבלים תמונה סטטית בלי זום, בלי פינים, ובלי יכולת לעדכן.

**מה כן מותר להוריד:** נתוני **OpenStreetMap** פתוחים תחת רישיון ODbL. אפשר
להוריד חילוץ אזורי מ-[Geofabrik](https://download.geofabrik.de), לייצר ממנו
אריחים משלך, ולארח אותם. הכלי הנוח לזה הוא
[Protomaps](https://protomaps.com) — קובץ `.pmtiles` יחיד שאפשר להעלות ל-Firebase
Hosting ולהגיש ישירות, בלי שרת אריחים. שים לב למכסה החינמית של Spark:
10GB אחסון אבל **360MB תעבורה ליום**, אז חילוץ של עיר אחת סביר, של מדינה שלמה לא.

בפועל, לצרכים שלך אריחי CARTO פשוט יותר ובאותה איכות.

---

## 2. סוכן ה-AI — כרגע לא צריך מפתח, כי הוא לא אמיתי

ב-[Chat.jsx](src/screens/Chat.jsx) התשובות קבועות מראש ומופיעות אחרי `setTimeout`.
אין קריאת רשת, אין מפתח, אין עלות. זה מספיק לאב-טיפוס ולהדגמה.

### כדי שיהיה אמיתי

**המפתח לא יכול לשבת בפרונטאנד.** זה שונה ממפתח מפות: מפתח מפות מוגן בהגבלת
referrer, אבל מפתח LLM שנחשף מאפשר לכל אחד לצרוב את המכסה שלך. הוא חייב לשבת
בשרת שמתווך בין האפליקציה ל-API.

זה המקום שבו "בחינם" נהיה מסובך: **Firebase Cloud Functions דורש את תוכנית Blaze**
(תשלום לפי שימוש), שדורשת כרטיס אשראי. אז אם רוצים להישאר בחינם לגמרי, הפרוקסי
צריך לרוץ במקום אחר:

| אפשרות | מכסה חינמית | כרטיס אשראי |
|---|---|---|
| Cloudflare Workers | 100,000 בקשות ליום | לא נדרש |
| Vercel Functions | Hobby plan | לא נדרש |
| Netlify Functions | 125,000 קריאות לחודש | לא נדרש |

ולמודל עצמו, **Google AI Studio (Gemini API)** מציע free tier אמיתי בלי כרטיס
אשראי, עם הגבלת קצב. Anthropic ו-OpenAI דורשים תשלום מראש.

**ארכיטקטורה מומלצת בחינם:** האפליקציה ב-Firebase Hosting → קריאה ל-Cloudflare
Worker → Worker מחזיק את מפתח Gemini כ-secret וקורא ל-API. שתי המכסות חינמיות
ואף אחת לא דורשת כרטיס.

---

## 3. שיתוף וסנכרון

### מה כבר עובד

**שיתוף בוואטסאפ אמיתי לחלוטין.** `wa.me` הוא קישור https רגיל — בלי SDK, בלי
מפתח, בלי WhatsApp Business API. הוא פותח את האפליקציה עם ההודעה מוכנה ונותן
לשולח לבחור צ'אט. הקוד ב-[src/lib/share.js](src/lib/share.js). היכן שיש
`navigator.share` (מובייל) נפתחת מגירת השיתוף של מערכת ההפעלה במקום, שממילא
כוללת את וואטסאפ.

**פיצול לפי אדם או משפחה עובד.** ב-[data.js](src/data.js) יש `FAMILIES`, וכל
עצירה מחזיקה `who` — אילו משפחות משתתפות בה. במסך הבית יש סינון לפי משפחה שמצמצם
את הלו"ז לעצירות שאותה משפחה באמת הולכת אליהן, ובמסך הפיננסים יש מתג
**לפי אדם / לפי משפחה** שמשנה את חישוב החובות: לפי משפחה כל משק בית משלם חלק
אחד ללא קשר לגודלו.

### מה עדיין מדומה

**הסנכרון.** כרגע כל דפדפן מחזיק state משלו. "מסונכרן" בבר העליון הוא תווית.
הקישור מעביר קוד הצטרפות ב-URL, אבל אין שרת שמחזיק את המסלול.

### איך להפוך אותו לאמיתי — בחינם

**Cloud Firestore בתוכנית Spark של Firebase** נותן סנכרון אמיתי בזמן אמת בלי
כרטיס אשראי:

- 1GB אחסון
- 50,000 קריאות ליום
- 20,000 כתיבות ליום
- listeners בזמן אמת כלולים

לקבוצת טיול זה הרבה מעבר למספיק. אתה כבר בתוך Firebase, אז אין ספק נוסף להוסיף.

מודל נתונים מוצע:

```
trips/{tripId}
  ├── city, day, totalDays
  ├── families: [{ id, name, color, members }]
  ├── stops/{stopId}    → time, name, lat, lng, cat, who[]
  └── expenses/{expenseId} → title, payer, amount, splitBy
```

הקישור שנשלח בוואטסאפ כבר נושא `?trip=<id>`, ו-`invitedTripId()` ב-share.js כבר
קורא אותו — אלה נקודות החיבור. מה שחסר הוא `onSnapshot` שמאזין למסמך ומזרים
שינויים לכל המכשירים.

**נקודה שדורשת החלטה:** בלי הזדהות, כל מי שיש לו את הקישור יכול גם לערוך. זה
בסדר לקבוצת חברים, אבל צריך כללי אבטחה ב-Firestore שמגבילים כתיבה למסמך הטיול
הספציפי. Firebase Anonymous Auth חינמי וסוגר את הפער בלי לבקש מאף אחד להירשם.
